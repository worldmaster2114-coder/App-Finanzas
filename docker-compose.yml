version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app_50_30_20
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
